{
  "name": "@repo/config",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint \"src/**/*.ts\" --max-warnings=0",
    "test": "echo \"config: no tests yet (foundation)\" && exit 0"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "eslint": "^8.57.0"
  }
}
